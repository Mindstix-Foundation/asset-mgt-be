// Application constants
export const APP_CONSTANTS = {
  DEFAULT_PAGE_SIZE: 10,
  MAX_PAGE_SIZE: 100,
  JWT_EXPIRES_IN: '7d',
} as const;

// Database constants
export const DB_CONSTANTS = {
  DEFAULT_SKIP: 0,
  DEFAULT_TAKE: 10,
} as const;
