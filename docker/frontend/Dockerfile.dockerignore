# Build artifacts
node_modules
dist
dist-ssr
coverage

# Logs
logs
*.log
npm-debug.log*

# Environment files (Vite vars come via build args / compose)
.env
.env.local
.env.*.local

# Editor / OS
.vscode
.idea
.DS_Store
*.swp

# Git
.git
.gitignore
.gitattributes

# Tests / scanners
.scannerwork
sonar-project.properties
playwright-report
test-results
e2e
*.spec.ts
*.test.ts

# Docker (don't recursively copy itself)
Dockerfile
.dockerignore

# Project docs / data not needed in the image
README.md
*.md
screenshots
Prototype

# TS build info
*.tsbuildinfo
.vite
